# 変更履歴

このアカウントの運用で行った修正を、後から追えるように記録する。

自動コミット（`post:` / `collect:` / `review:`）は対象外。人が判断して入れた変更だけを書く。
週次ジョブによる `strategy.md` の自動更新は [strategy.md](strategy.md) の「変更履歴」セクションに記録される。

**書き方**

- **依頼** … 実際に出した指示を原文のまま残す。要約しない
- **調べたこと** … その指示を受けて何を確認し、何が原因だと判断したか
- **変えたこと** … 実際の変更内容

「なぜこのルールがあるのか」「この変更は効いたのか」を後から検証できるようにするため、
指示の言い回しと判断の根拠を省かずに書く。

---

## 2026-09-01 AIが書いた文章に見える特徴を潰す

`1eec0fe`

> **依頼**
> 「制限はなさそうです
> 投稿内容もっとブラッシュアップできない？
> おそらくAIがツイートしているなってバレてる感じするし、もう少し人格出した方がいい気がする」

**調べたこと** — 直近5投稿を読み直したところ、前回入れた一人称が「型」として消化されていた。

- 4本連続で第3段落が「自分は〜」「自分も〜」で始まっていた
- 5本すべてが3段落。前回「3段落固定にしない」と書いたが効いていなかった
- 全文が主語述語の揃った説明文。「〜のため」「〜がちです」「〜からです」が多用されていた
- 体験が「以前は悩んでいました」など、誰にでも書ける一般論だった
- 「無料から試せます」が末尾に付け足されており、宣伝文の型になっていた

**変えたこと**

- `content_prompt.md` に「AIが書いた文章に見えないために」を新設（7項目）
  - 一文を短くして長短のリズムをつける／語尾を全部「です・ます」にしない
  - 段落数を実際に変える／隙をなくさない（本音を書いてよい）
- 「自分は〜」で段落を始めるのは4投稿に1回までに制限し、入れ方を変えることを要求
- 体験は「その人にしか書けない具体」を必須に（いつ・どの場面・何が起きたか＋感情1語）
- `lexicon.md` に「説明調に見える言い回し」「末尾に付け足さない補足」を追加
- `strategy.md` の固定ルールにも反映（週次ジョブに消されないため）

**確認すべきこと** — 次の3〜4本で段落数がばらけるか、「自分は」始まりが減るか。

---

## 2026-09-01 図や表を出させる話では出力形式まで書く

`081ac03`

> **依頼**
> 「1時間のツイートは悪くないけど、このツイートにどの形式で図解するべきか？を書いてほしい。
> 今回でいえばHTMLファイル形式で図解 みたいな」
>
> （対象の投稿：「思考の壁打ちで抜け漏れを減らすなら、文章でやり取りするよりClaudeに
> 図解させるほうが確実です。…Claudeなら要点をマインドマップなどの図にして整理してくれます」）

**問題** — 「マインドマップなどの図にして整理してくれます」では読者が再現できない。
一見よさそうでも、実際には何も渡していない投稿になっていた。

**変えたこと** — `content_prompt.md` の「具体性の絶対条件」に5項目目を追加。
文章以外（図・表・チャート・資料）を出させる話では、出力形式を必ず本文に書く。

| 出したいもの | 頼み方 |
| --- | --- |
| 構造・関係の図解 | 「HTMLファイルで図解して」「Mermaid記法で書いて」 |
| 比較・一覧 | 「マークダウンの表で」 |
| 階層のある整理 | 「入れ子の箇条書きで」 |

---

## 2026-08-31 締めと書き出しの型を増やし、一人称を導入

`da22436`

> **依頼**
> 「ツイート分析して悪い部分改善してくれませんか？全然ツイートが良くないです」

**調べたこと** — メトリクスを確認したところ、18投稿・累計1,082 views で
**いいね0・リポスト0・フォロワー0**（反応率0.08%）。
表示はされている（133、226 viewsの回もある）のに反応がゼロだったため、
露出ではなく内容側の問題と判断し、文章のパターンを機械的に数えた。

| 症状 | 直近10本 |
| --- | --- |
| 締めが「〜します/なります」 | 10/10本 |
| 締めの結論が「時間・手間が減る」 | 7/10本 |
| 「〜だけ」を使用 | 6/10本 |
| 段落が3つ固定 | 10/10本 |
| 一人称の使用 | 0/10本 |

柱も業務も変えたのに、文章のリズムが完全に同一だった。
ペルソナが「実務者」なのに一人称がゼロで、説明書の口調になっていた。

**変えたこと**

- 締めの型を5種類定義し、毎回変えることを必須に（「〜が省けます」系は5本に1本まで）
- 一人称を明示的に許可（3〜4投稿に1回、実感を入れる）
- A/Bパターンを2種→4種に拡張（状況の言語化／結果提示／思い込みを外す／問いを立てて自分で答える）
  - 従来はAとBの交互で、書き出しが2パターンしかなく完全に予測可能だった
- 段落数の固定を解除
- 根拠のない数字を禁止（「2時間が15分」は測っていない数字だった）
- 「AIを少し使っている人には既知の内容」で終わらせない要件を追加

---

## 2026-08-28 ツール紹介と注意点の柱を追加

`f48648b`

> **依頼**
> 「うーん直近のツイートは以下だと思うけど、なんか限定的なんだよなあ
> 50ページ以上とか買うべきだし、ピンポイントすぎるし、プロンプトしか書かないのは良くないよなあ
> おすすめのAIツールの紹介とかAIツールの正しい使い方とかもう少し幅広く紹介してくれる？」
>
> （対象の投稿：「50ページのPDF資料は、条件を3つ指定するだけで要点の見落としが防げます。…」）

**調べたこと** — 3点とも設定側に原因があった。

1. `content_prompt.md` の「職種・状況まで踏み込む。広く呼びかけるほど誰にも刺さらない」
   というルールに従い、AIが数値で条件を付けて対象を狭めていた
2. 既存5柱のうち3つがプロンプト提示型で、実質ほぼ全投稿にプロンプトが載る構造だった
3. `strategy.md` に「ツールの優劣・おすすめ・比較は書かない」と明記されており、
   **構造上ツール紹介が不可能**だった

**変えたこと**

- 柱を5→7に拡張。`AIツール紹介` と `AIの落とし穴と対処` を追加（どちらもプロンプトを載せない型）
- プロンプトを載せてよい柱を2つに限定。巡回順もプロンプト型が連続しないよう設計
  - シミュレーションで21本中6本（28%）まで低下、連続ゼロを確認
- ツール紹介の線を引き直し（何ができるか・無料か・使い分けはOK／煽り・ランキング・
  他ツールを下げる比較・登録誘導はNG）
- 数値で対象を絞ることを明示的に禁止（「50ページ以上の」「1時間超の」など）
- 種は 7×28×7 = 1372件（約455日分）

---

## 2026-08-28 柱を確実に巡回させ、業務シーンを28種に拡大

`aca2674`

> **依頼**
> 「うーん全然見られないなあ
> 普通に投稿がありきたりすぎるし、単調すぎない？
> もっと幅広く役立つAI活用の情報を投稿するようにしないと」
> 「色々な業務でのAIの使い方を説明しないと」

**調べたこと（根本原因）** — 2日前に整備した「柱ごとの型」が**ほとんど使われていなかった**。

| 柱 | 9本中の使用 |
| --- | --- |
| 即実践プロンプト例 | 6本 |
| よくある失敗と改善策 | 2本 |
| 業務別時短Tips | 1本 |
| 数字で示す効果 | **0本** |
| あるある＋即Tips | **0本** |

種は「未使用のうち先頭」から順に消費される実装で、順番は生成時のシャッフル結果のまま。
つまり**柱の巡回が運任せ**だった。差別化の要である共感入口型と数字型が一度も発火せず、
実質1つの型だけで9本書いていた。

**変えたこと**

- `post.py` の種選択を書き換え。A/Bパターンと同じく投稿数から次の柱を機械的に決定
- 直近4本と同じ業務シーンを避ける選択を追加（`報告書・日報` が3本重複していたため）
- 業務シーンを12→28種に拡大。文書作成偏重を解消し、読む・数える・整える・考える・
  伝える・段取りの系統を追加（Excel関数、PDFを読む、英語メール、クレーム対応など）
- 詰まりポイントも見直し。`書き出しで止まる` `何を書けばいいか迷う` は文章作成専用の表現で
  Excelやリサーチに当てはまらないため、汎用の7種に置換。
  `そもそもAIが使えると思っていない` を追加

---

## 2026-08-26 柱ごとに投稿の型を定義

`128e09e`

> **依頼**
> 「Threadsのつぶやきに関してまだ少しありきたりな感じなので、以下の内容を踏まえて、
> プロンプトを改善してくれますか？」
>
> あわせて、伸びやすい5パターンの資料が提供された。
> ①あるある＋即Tips型 ②コピペプロンプト型 ③数字・時短効果型 ④失敗あるある＋改善型
> ⑤業務ピンポイント型。投稿のコツとして「1行目で止める」「1投稿1ポイント」
> 「100〜250文字程度」「コピペしてすぐ使える形にする」「過度な自慢やアフィ臭は避ける」。

**調べたこと** — **設定ファイルが定型句を「良い例」として指示していた。**

`content_prompt.md` に `✅ お使いのAIチャット（ChatGPT・Gemini・Copilotなど）にこう貼ります`
と書かれており、AIは忠実に従っていた。結果、**5投稿中4本が一字一句この24文字を含んでいた**。
ツール中立にする意図は正しかったが、実装が「毎回同じ節を挿入する」形になっていた。

また5つの柱が一行の説明しかなく型の区別がないため、柱が違っても構文が同じだった。
文字数も258/268/335/297字と、指針の100〜250字を大きく超えていた。

**変えたこと**

- 提供された5パターンを柱と1対1で対応させ、それぞれ構造・1行目の作り方・具体例を定義
- 柱を1つ改名（`小さな習慣化Tips` → `あるある＋即Tips`）
  - 提供リストにこの型がなく、習慣化・心構えの話は固定ルールの「精神論禁止」と衝突して
    抽象化の温床になっていた
  - 共感入口型はThreadsで最も効く型なので、そこを空けておく理由がない
- 文字数を柱で変える（柱1のみ350字、他は100〜250字）
  - 一律に短くするとコピペプロンプト型はプロンプト全文が入らず価値が消えるため
- `lexicon.md` に「毎回同じ文になる定型句」の禁止セクションを新設
- A/Bパターンの A を「対象呼びかけ型」から「状況の言語化型」に変更（呼びかけ型が飽和）

---

## 2026-08-25 コンテナの準備完了を待ってから公開する

`62553b7`（v1にも同じ修正を適用）

> **依頼**
> 「それとは別でv2の実行したら12:38に実行したやつはちゃんと実行できたけど、
> 07:43は以下のエラーになった」（`ThreadsApiError: HTTP 400 ... "Media Not Found"`）
> 「これどういう意味？」
>
> v1にも同じ不具合がある旨を報告したところ「同じ修正して」との指示。

**原因** — Threads APIの投稿は「①コンテナ作成 → ②公開」の2段階だが、
①の直後は②がまだコンテナを見つけられないことがある。実装が①②を待ち時間ゼロで
連続実行していたため、タイミング次第で失敗していた。
エラーには `is_transient: false` と入っているが、実際にはタイミング依存の一時的な失敗。

**変えたこと** — コンテナの `status` が `FINISHED` になるまで待ってから公開する
（3秒間隔、最大90秒）。固定秒数のsleepにしなかったのは、準備が速いときに無駄に待たないため。
作成直後は status 取得自体も同じエラーを返すことがあるため、その場合は待ち続ける。

モックで検証（即FINISHED／2回未反映→FINISHED／IN_PROGRESS→FINISHED／ERROR／
EXPIRED／認証エラー／タイムアウト）。

---

## 2026-08-25 ツールをChatGPT前提にしない

`b9db5f0`

会社員・公務員は使えるAIが会社で決まっていることが多く、ChatGPT前提で書くと
読めない読者が大量に出るため、ツール非依存の書き方に変更。

**注**: この変更が意図せず定型句を生み、`128e09e` で修正することになった。
方針は正しかったが、実装（✅の例文）が悪かった。

---

## 2026-08-24 投稿が抽象的になる原因を潰す

`a5bd900`

「具体性の絶対条件」4項目を追加。プロンプトは全文を出す、何を貼るかを書く、など。

**注**: この時に入れた「職種・状況まで踏み込む」が、後に数値で対象を狭める副作用を生み、
`f48648b` で修正することになった。

---

## 2026-08-24 公開リポジトリ向けに認証情報の漏洩対策を強化

`3c0a15d`

> **依頼**
> 「これpublicにしたけど、このままpublicにしてまずいものはないですか？
> 書き換えて欲しいです。認証情報とか」

**調べたこと** — 全コミットの全blobを走査した結果、キー・トークンの混入はゼロだった。

**見つかった問題** — Publicだと**Actionsの実行ログも誰でも読める**。
`collect.py` にはトークンを伏せる処理があったが、`post.py` と `review.py` には無かった。

**変えたこと** — `_secrets.py` にマスク処理を集約。例外メッセージ・APIレスポンス・
外部コマンド出力をログに出す箇所は必ず `redact()` を通す。未捕捉の例外も `run_safely()` で
受けてトレースバックをマスクしてから出力する。

**あわせて対応** — コミット履歴に個人のGmailアドレスが残っていたため、
`main` にファイルを移し替えて該当ブランチを削除する形で解消（履歴の書き換えは不要だった）。
また、ワークフローがデフォルトブランチに無いと cron が発火しないため、`main` へ移動した。

---

## 2026-08-24 GitHub Actionsによる自動投稿を追加し、生成をGeminiに切り替え

`2fd4cc0`

> **依頼**
> 「ai-threads-fortune-v1みたいに自動投稿の設定をしたいんだけど、
> ai-threads-fortune-v2はまだしてません。どういう設定をすればいいですか？」
> 「github actionsの設定とかもしないといけないかも」「settingの変数設定とか」
>
> 続けて「ごめんやっぱANTHROPIC_API_KEYを使わないで、GEMINI_API_KEYにします。
> それに合わせて関連する部分も修正してください」
> さらに「無料枠で投稿する予定なので、その前提でお願いします」

**やったこと**

- `.github/workflows/` を新規作成（`daily.yml` / `weekly.yml` / `setup.yml`）
  - cronは各スロットの15分前に起動し、ランダム待機で投稿時刻を散らす
  - scheduleはデフォルトブランチのワークフローしか実行しないため、mainに配置
- 生成をAnthropic APIからGemini APIに変更（`GEMINI_API_KEY`）
  - JSON出力の指定方法、生成拒否の検出、レート制限の判定がそれぞれ異なるため個別対応
- 無料枠向けの対応
  - 429の「1日の上限(PerDay)」と「1分あたりの上限(PerMinute)」を区別し、
    待って直るものだけリトライする
  - 503 UNAVAILABLE は無料枠だと当たりやすいため、リトライ回数と待ち時間を多めに取る

---

## 運用上のメモ

### 未解決の論点

**フォロワーが増えていない。** 2026-09-01時点で23投稿・累計1,209 views・
いいね0・リポスト0・フォロワー0。内容の改善を4回行ったが、数字は動いていない。

現在の設計は「返信・絡みを想定しない」一方通行の投稿。
フォロワーがいる状態を維持するには機能するが、ゼロから増やす力はない。
内容の改善だけで解決するかは不明で、人力での立ち上げ（同ジャンルへの返信など）が
必要な可能性がある。

なお Threads 側のアカウント制限は無いことを確認済み。

### トークンの期限

Threadsの長期トークンは60日で失効する。
**2026-08-24発行 → 2026-10-23失効。10月16日頃までにリフレッシュが必要。**

```bash
curl -s "https://graph.threads.net/refresh_access_token?grant_type=th_refresh_token&access_token=$LONG"
```

失効前かつ発行から24時間以上経過していれば延長できる。現状この作業は自動化されていない。

### 設定を変えるときの注意

柱の名前は次の4箇所で一致させる必要がある。ズレると巡回が空振りし、同じ柱ばかりになる。

- `seeds.jsonl` の `pillar`
- `scripts/build_seeds.py` の `PILLARS`
- `scripts/post.py` の `PILLAR_ROTATION`
- `content_prompt.md` の「発信の柱」

`post.py` は起動時にズレを検知して警告を出す。

文字数などのルールは `content_prompt.md` と `strategy.md` の両方に書かれている。
片方だけ変えると生成が不安定になるため、必ず両方を揃える。

### これまでの教訓

**投稿の質に問題があるとき、原因はプロンプトの文言ではなく仕組み側にあることが多い。**

- 定型句の反復 → 設定ファイルが定型句を「✅良い例」として指示していた（`128e09e`）
- 柱の偏り → 種の消費順がシャッフル任せで、巡回していなかった（`aca2674`）
- ツール紹介が出ない → 固定ルールで禁止されていた（`f48648b`）
- 対象の絞りすぎ → 「職種まで踏み込む」ルールの副作用だった（`f48648b`）

投稿を読んで違和感があったら、まず設定ファイルに該当する指示が無いかを確認する。
