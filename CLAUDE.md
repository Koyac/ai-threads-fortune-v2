# CLAUDE.md

このリポジトリで作業する際のガイドライン。

## 変更履歴を必ず残す

**設定やコードに修正を入れたら、[CHANGELOG.md](CHANGELOG.md) に追記してから commit する。**

記録する形式は次の3点。CHANGELOG.md の冒頭にも書いてある。

- **依頼** … 受け取った指示を**原文のまま**引用する。要約しない
- **調べたこと** … 何を確認し、何が原因だと判断したか（数字があれば数字で）
- **変えたこと** … 実際の変更内容

投稿の質に関する修正は、後から「この変更は効いたのか」を検証する必要がある。
判断の根拠と、そのときの投稿の状態（メトリクスやパターンの出現数）を省かずに書くこと。

自動コミット（`post:` / `collect:` / `review:`）は記録しない。人が判断した変更だけ。

## 設定ファイルの関係

投稿の生成には次の3ファイルがまるごとプロンプトに埋め込まれる。**内容が矛盾すると生成が不安定になる。**

| ファイル | 役割 |
| --- | --- |
| `content_prompt.md` | 生成方針の本体。柱ごとの型、具体性の条件、禁止事項 |
| `strategy.md` | 固定ルールとA/Bパターン。週次ジョブが一部を書き換える |
| `lexicon.md` | 語彙・語尾・禁止表現 |

文字数などのルールは `content_prompt.md` と `strategy.md` の両方に書かれているため、
**片方だけ変えない**。

`strategy.md` の「固定ルール」セクションは週次ジョブが変更してはいけない領域で、
`review.py` が保存前に検証している。人が編集するのは問題ない。

## 柱の名前は4箇所で一致させる

ズレると柱の巡回が空振りし、同じ柱ばかりが選ばれる（実際に起きた。CHANGELOG の `aca2674` 参照）。

- `seeds.jsonl` の `pillar`
- `scripts/build_seeds.py` の `PILLARS`
- `scripts/post.py` の `PILLAR_ROTATION`
- `content_prompt.md` の「発信の柱」

`post.py` は起動時にズレを検知して警告を出す。

種を作り直すときは、**使用済みの組み合わせを引き継ぐ**こと。引き継がないと同じ内容が再投稿される。

## 投稿の質を直すとき

**原因はプロンプトの文言ではなく仕組み側にあることが多い。**
違和感のある投稿を見つけたら、まず設定ファイルに該当する指示が無いかを確認する。

実際にあった例：

- 定型句が毎回入る → 設定が定型句を「✅良い例」として指示していた
- 特定の柱ばかり出る → 種の消費順がシャッフル任せで巡回していなかった
- ツール紹介が出ない → 固定ルールで禁止されていた
- 対象を絞りすぎる → 「職種まで踏み込む」ルールの副作用だった

推測で直す前に、`data/posts.jsonl` の実物を読み、パターンの出現数を数えること。

## 公開リポジトリである

このリポジトリはPublicで、**GitHub Actions の実行ログも誰でも読める。**

例外メッセージ・APIレスポンス・外部コマンドの出力をログに出す箇所は、
必ず `scripts/_secrets.py` の `redact()` を通すこと。
`main()` は `run_safely()` で包み、未捕捉の例外のトレースバックもマスクする。

認証情報をコードに書かない。すべて GitHub Secrets 経由で環境変数として渡す。

## ワークフローはデフォルトブランチに置く

GitHub Actions の `schedule`（cron）は**デフォルトブランチのワークフローしか実行しない**。
feature ブランチに置いても永久に発火しない。
